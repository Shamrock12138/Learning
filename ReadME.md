`Knowledge`文件夹为各个知识库
  `ReinforcementLearning`为强化学习相关的知识库，代码演示可在当前文件夹下的`test.py`中看到
`Projects`文件夹为各个模块（重要实现）
  `myTools`已经封装成包（setup.py），包含各个模型实现，若要使用，先使用`pip install .`安装
    `Model/RL.py`为强化学习的相关模型
    `Utils/...`为服务各个模型、实现的工具函数

`shamrock_pc shamrock_laptop`分支为最新分支，目前还在对整体结构修改，可能造成代码无法运行的情况，可以查看`main`分支，`main`分支代码可以顺利运行。
